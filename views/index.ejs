<!DOCTYPE html>
<html lang="en">
<head>

    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>WeatherTrack</title>
    <link rel="stylesheet" type="text/css" href="/styles/main.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Lora:ital,wght@1,700&family=Roboto:wght@500&display=swap" rel="stylesheet">

</head>

<body>
    <div class="container">

    <%# HTML code for JS Post method --> input, search button %>
        <form method="post" action="/">
            <div class="search">
                <input name = "cityName" type="search" placeholder="Enter city name" spellcheck="false">
                <button type="submit"><img src="/images/search.png"></button>
            </div>
        </form>

        <% if(locals.temp) {%>
        <div class="weather">

        <%# HTML code for main data --> temperature, city name and picture %>
            <img src="<%= image %>">
            <h1> <%= temp %>°C </h1>
            <h2> <%= city %> </h2>
        
            <div class="details">

            <%# HTML code for humidity icon and value %>
                <div class="col">
                    <img src="/images/humidity.png">
                    <div>
                        <p class="humidity"> <%= hum_value %>% </p>
                        <p>Humidity</p>
                    </div>
                </div>
            
            <%# HTML code for wind icon and value %>
                <div class="col">
                    <img src="/images/wind.png">
                    <div>
                        <p class="wind"> <%= wind_value %> m/s </p>
                        <p>Wind Speed</p>
                    </div>
                </div>

                </div>
            </div>
        </div>

        <%# Until we pressed search button and started JS Post method %>
        <% }else {%>
            <p class="wait">Waiting for data...</p>
            <% } %>
    </div>
</body>

</html>